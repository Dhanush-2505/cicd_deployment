apiVersion: v1
kind: Service
metadata:
  name: app_alpha-svc
  labels:
    app: app_alpha
spec:
  type: LoadBalancer
  selector:
    app: app_alpha
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
